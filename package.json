{
  "name": "mkwo-map-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Static hosting for MKWO Map Tracker",
  "scripts": {
    "start": "live-server --port=8080 --host=0.0.0.0 --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
